import { useEffect, useState } from 'react'
import AcaoCard from '../components/AcaoCard'
import SectionTitle from '../components/SectionTitle'
import { acoes } from '../data/acoes'
export default function Gamificacao(){const [concluidas,setConcluidas]=useState<number[]>([]);const [mensagem,setMensagem]=useState('Escolha uma ação para começar.');const pontos=acoes.filter(a=>concluidas.includes(a.id)).reduce((s,a)=>s+a.pontos,0);useEffect(()=>{setMensagem(pontos===0?'Escolha uma ação para começar.':pontos>=150?'Excelente! Você já passou de 150 pontos sustentáveis.':`Você já conquistou ${pontos} pontos. Continue!`)},[pontos]);const toggle=(id:number)=>setConcluidas(v=>v.includes(id)?v.filter(x=>x!==id):[...v,id]);return <section className="mx-auto max-w-7xl px-4 py-16 md:px-8"><SectionTitle eyebrow="Solução 01" title="Gamificação sustentável" description="Experimente o protótipo: conclua ações, acumule pontos e acompanhe seu progresso em tempo real."/><div className="mt-10 grid gap-8 lg:grid-cols-[1fr_320px]"><div className="grid gap-4 md:grid-cols-2">{acoes.map(a=><AcaoCard key={a.id} acao={a} concluida={concluidas.includes(a.id)} onToggle={toggle}/>)}</div><aside className="h-fit rounded-3xl bg-ink p-7 text-white lg:sticky lg:top-24"><p className="text-sm font-bold uppercase tracking-widest text-brand-400">Progresso</p><p className="mt-3 text-5xl font-black">{pontos}</p><p className="text-slate-400">pontos acumulados</p><div className="mt-6 h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-brand-400 transition-all" style={{width:`${Math.min(pontos/230*100,100)}%`}}/></div><p className="mt-4 text-sm leading-6 text-slate-300">{mensagem}</p></aside></div></section>}
