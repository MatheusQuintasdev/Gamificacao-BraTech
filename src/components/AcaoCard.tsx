import type { AcaoSustentavel } from '../types'
interface Props{acao:AcaoSustentavel;concluida:boolean;onToggle:(id:number)=>void}
export default function AcaoCard({acao,concluida,onToggle}:Props){return <article className={`rounded-2xl border p-5 transition ${concluida?'border-brand-400 bg-brand-50':'border-slate-200 bg-white'}`}><div className="flex items-start justify-between gap-4"><div><span className="text-xs font-bold uppercase tracking-wide text-brand-600">{acao.categoria}</span><h3 className="mt-1 text-lg font-bold">{acao.titulo}</h3></div><span className="rounded-full bg-ink px-3 py-1 text-sm font-bold text-white">+{acao.pontos}</span></div><p className="mt-3 text-sm leading-6 text-slate-600">{acao.descricao}</p><button onClick={()=>onToggle(acao.id)} className="mt-4 text-sm font-bold text-brand-600 hover:underline">{concluida?'Desmarcar ação':'Concluir ação'}</button></article>}
